\### Atomic Red Team:

`Invoke-AtomicTest T1003.001-1,2,5,10`





\### MITRE ATT\&CK :

* Credential Access (TA0006)

  * OS Credential Dumping (T1003)



\### Description:

This detection queries for the use of ProcDump.exe with the flag "-ma" on the process "lsass.exe". 



`event.dataset:("endpoint.events.process" or "windows.sysmon\_operational"  ) and process.args: (\*procdump\*\*\* and \*-ma\*\*\*  and \*lsass.exe\*\*\*)`



\### Description:

This detection queries for the use of "Rundll32.exe" to run "comsvcs.dll" with the function "MiniDump".



`event.dataset:("endpoint.events.process" or "windows.sysmon\_operational"  ) and process.args: (\*comsvcs.dll\*\*\* and \*MiniDump\*\*\*) and process.name :  "rundll32.exe"`



\### Description:

This detection queries Sysmon `ProcessAccess` logs and detects interaction with "lsass.exe" with elavated permissions. 



`event.code:"10" winlog.event\_data.TargetImage : \*lsass.exe\* winlog.event\_data.GrantedAccess: ("0x1010" or "0x1F0FFF" or "0x1fffff")`

